import React from "react";
import { ScrollView, Text, View, StyleSheet } from "react-native";

export const PreWorkoutView = (props: any) => {
    return (
        <ScrollView>
            <Text>Aquecimento</Text>
        </ScrollView>
    );
};